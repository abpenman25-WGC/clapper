import * as THREE from 'three';

import { SvgShape } from './types';
import { degToRad } from 'three/src/math/MathUtils.js';

export function SvgShapeMesh(props: any) {
  // TEMPORARILY DISABLED FOR REACT 19 MIGRATION
  return null;
};