{
  "name": "@aitube/engine",
  "module": "index.ts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "version": "0.2.4",
  "description": "The engine powering AiTube.at and Clapper.app",
  "scripts": {
    "build": "tsc && tsc-alias -p tsconfig.json && pnpm run build:declaration",
    "build:declaration": "tsc --emitDeclarationOnly --project tsconfig.types.json",
    "postbuild": "rimraf tsconfig.types.tsbuildinfo && pnpm run build:declaration",
    "publish": "npm publish --access public",
    "update": "rm -Rf node_modules && rm pnpm-lock.yaml && pnpm i && pnpm run build"
  },
  "dependencies": {
    "@aitube/clap": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "^1.3.10",
    "prettier": "^3.8.1",
    "rimraf": "^6.1.3",
    "tailwindcss": "4.2.1",
    "tsc-alias": "^1.8.16",
    "typescript": "5.9.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jbilcke-hf/aitube-engine.git"
  },
  "keywords": [
    "Clapper.app",
    "OpenClap",
    "AI cinema",
    "AiTube"
  ],
  "author": "Julian Bilcke",
  "license": "MIT",
  "files": [
    "dist/*.js",
    "dist/*.d.ts",
    "dist/**/*.d.ts"
  ]
}
